{
    // Optional. The name or path of the coding standard to use. Defaults to the one set in phpcs global config.
    "phpcs.standard": "PSR2",
    // Optional. A comma separated list of patterns to ignore files and directories.
    "phpcs.ignore": "*.blade.php,/vendor/,/config/,/database/,/storage/"
}